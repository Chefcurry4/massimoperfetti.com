Drop the CV here with this exact filename:

  Perfetti Resume.pdf

Served at /cv/Perfetti%20Resume.pdf (the space is URL-encoded). The
sidebar CV icon links to that path via src/data/contacts.ts.

If you rename the file, update the matching `url:` field in
src/data/contacts.ts so the icon still resolves.
